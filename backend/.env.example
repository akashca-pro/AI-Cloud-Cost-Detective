# Copy to .env and fill in values. Never commit .env.

OPENAI_API_KEY=

# Amazon RDS PostgreSQL (WP3)
# Example local: postgresql://postgres:postgres@localhost:5432/cost_detective
DATABASE_URL=

# JWT signing secret (WP4) — use a long random string in production
JWT_SECRET=
